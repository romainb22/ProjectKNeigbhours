#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include<time.h>
#include "point.h"
#include "tableau.h"
#include "load.h"
#define MAX_LINE_LENGTH 15

int main(){
  srand(time(NULL));
  tableau *tab;
  tab = loadFromfile("./example/ex1.txt");

exit(EXIT_SUCCESS);
}
